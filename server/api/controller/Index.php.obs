<?php

namespace App\controller;

use Psr\Http\Message\ResponseInterface as Response;
use Psr\Http\Message\ServerRequestInterface as Request;

class Index
{

    public static function get( Request $request, Response $response, array $args ) : Response {
		$api = [];
		$api[] = [ 'url'=>'/api/2/standard', '?'=>'provides complete bdrg standard tree of sections, subsections, breeds and colors' ];
		$api[] = [ 'url'=>'/api/2/district', '?'=>'provides complete bdrg districts tree' ];

//        $response->getBody()->write(json_encode(['api' => $api], JSON_UNESCAPED_SLASHES));
        global $apiIndex; // to get access to the global
        $response->getBody()->write( json_encode([
			'title'=> 'Index for RGZuchbuch.de api',
			'standard' => 'https://rgzuchtbuch.de/api/standard'
		] ) );
        return $response;
    }




}
